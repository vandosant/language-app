#content_container
  .form-container
    = form_for @user, :url => {:action => "create"} do |f|

      .errors
        -if @user.errors.any?
          -@user.errors.each do |_, error|
            %p
              =error

      .field-container
        =f.label :email
        =f.text_field :email

      .field-container
        =f.label :password
        =f.password_field :password

      .form-actions
        =f.submit "Register"